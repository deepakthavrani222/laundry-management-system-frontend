'use client'

// This page renders the help content within the tenant context
// The TemplateHeader will automatically use tenant branding via TenantContext

import HelpPageContent from '@/app/help/page'

export default function TenantHelpPage() {
  return <HelpPageContent />
}
